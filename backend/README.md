# SafeRoute NYC Backend

This directory will contain the Flask application responsible for authentication, routing proxies, crime preprocessing, and panic notifications. Use this file to document setup steps (virtualenv, env vars, database notes) when implementation starts.
