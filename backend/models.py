"""SQLAlchemy model layer describing SafeRoute NYC domain entities.

The actual classes defined here will be used by ``backend/routes/auth.py`` for
user creation/login, ``backend/routes/panic.py`` for fetching trusted contacts,
and background scripts like ``scripts/seed_database.py`` when seeding demo data."""
