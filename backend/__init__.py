"""Marks the SafeRoute NYC backend as a Python package so ``backend.app`` can
expose the Flask application factory and other modules (routes, services,
models) can be imported using the ``backend.*`` namespace."""
