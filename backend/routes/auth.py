"""Blueprint stub for authentication endpoints consumed by React auth pages.

When implemented, these handlers will call ``backend.services.auth`` for
password hashing + JWT generation, read/write ``backend.models.User`` records via
``backend.database`` sessions, and power the frontend ``LoginPage``/
``RegisterPage`` forms."""
