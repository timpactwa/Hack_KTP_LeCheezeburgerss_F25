// Vite injects this entry file, which will render <App /> inside #root, wrap it
// with providers (React Query, AuthContext), import global styles, and hydrate
// any Mapbox CSS assets before the UI communicates with the Flask backend.
