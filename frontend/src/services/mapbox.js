// Will export functions to create Mapbox GL JS instances using the VITE_MAPBOX_TOKEN
// from Vite config, define shared sources/layers, and expose helpers consumed by
// MapDashboard + CrimeHeatmapLegend to keep map setup consistent.
