// Collects signup fields, optional trusted contact info, and posts them to the
// Flask /register endpoint via api.js. On success it will hydrate AuthContext
// and route users into the dashboard experience.
