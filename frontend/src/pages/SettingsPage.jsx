// Dashboard page where users will edit trusted contacts, panic message copy,
// and routing defaults by calling future backend endpoints (e.g. /settings,
// /trusted-contacts). Saves feed back into AuthContext + PanicButton UI.
