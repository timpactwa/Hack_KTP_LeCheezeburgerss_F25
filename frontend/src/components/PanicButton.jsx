// Floating action button visible on all authenticated views; when pressed it
// will pull auth tokens from AuthContext, the latest coords from useGeolocation,
// and invoke api.js -> /panic-alert before surfacing confirmation to the user.
