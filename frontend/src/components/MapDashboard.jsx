// Responsible for creating the Mapbox GL JS map instance, registering layers
// for the crime heatmap + avoid polygons returned by Flask, and syncing the
// user's geolocation (from useGeolocation + PanicButton) with displayed routes.
