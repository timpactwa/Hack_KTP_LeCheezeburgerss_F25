// Handles address/coordinate inputs, triggers geocoder/autocomplete widgets,
// and invokes the useSafeRoute hook so the backend's /safe-route response can be
// fed into MapDashboard + RouteComparisonPanel.
