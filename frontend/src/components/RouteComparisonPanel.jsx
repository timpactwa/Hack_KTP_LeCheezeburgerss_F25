// Consumes data returned by useSafeRoute/api.js and surfaces metrics for the
// shortest vs safest paths. Selecting a card will notify MapDashboard which line
// to emphasize and inform PanicButton which path is active.
