# SafeRoute NYC Frontend

This directory will eventually hold the React + Mapbox client. Keep build instructions, component diagrams, and any deployment caveats here so the rest of the team can quickly bootstrap the UI layer during the hackathon.
